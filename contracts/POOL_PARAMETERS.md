# contracts/POOL_PARAMETERS.md
# TON Nominator Pool - Параметри для зміни

## 🎯 Поточні обмеження (Офіційний контракт)

### В файлі `func/new-pool.fif`:

```fift
max_nominators_count 40 > abort"we do not recommend max_nominators_count higher than 40"
```
- **Максимум учасників:** 40
- **Проблема:** Дуже мало для публічного пулу

```fift
min_validator_stake 1000000000000 < abort"we do not recommend min_validator_stake less than at least 1000"
```
- **Мінімальна ставка валідатора:** 1,000 TON (1000000000000 nanoton)
- **Проблема:** Дуже велика сума

```fift  
min_nominator_stake 100000000000 < abort"we do not recommend min_nominator_stake less than at least 100"
```
- **Мінімальна ставка номінатора:** 100 TON (100000000000 nanoton)
- **Проблема:** Недоступно для звичайних користувачів

---

## ✅ Рекомендовані зміни (Для публічного пулу)

### Нові параметри:

| Параметр | Було | Стане | Причина |
|----------|------|-------|---------|
| **max_nominators_count** | 40 | **1000** | Більше учасників |
| **min_validator_stake** | 1000 TON | **10 TON** | Доступніше |
| **min_nominator_stake** | 100 TON | **10 TON** | Для всіх |

### Код після змін:

```fift
max_nominators_count 1000 > abort"max_nominators_count cannot be higher than 1000"
min_validator_stake 10000000000 < abort"min_validator_stake cannot be less than 10 TON"
min_nominator_stake 10000000000 < abort"min_nominator_stake cannot be less than 10 TON"
```

---

## 📝 Інструкція по застосуванню

### Автоматичний патч (рекомендовано):

```powershell
cd contracts
.\patch_pool_parameters.ps1
```

### Ручна зміна:

1. Відкрийте `contracts/repo/func/new-pool.fif`
2. Знайдіть рядок ~20: `max_nominators_count 40 >`
   - Змініть `40` на `1000`
3. Знайдіть рядок ~22: `min_validator_stake 1000000000000 <`
   - Змініть `1000000000000` на `10000000000` (10 TON)
4. Знайдіть рядок ~24: `min_nominator_stake 100000000000 <`
   - Змініть `100000000000` на `10000000000` (10 TON)
5. Збережіть файл

---

## ⚠️ Важливі примітки

### Безпека:
- ✅ Ці зміни **НЕ впливають** на immutable патч (validator_address = 0)
- ✅ Змінюють тільки **параметри ініціалізації** пулу
- ✅ Після деплою контракту ці значення **незмінні**

### Обмеження TON:
- Мінімум для стейкінгу в elector: **10,000 TON**
- Наш пул буде збирати кошти, але для участі у валідації потрібна велика сума
- **Рішення:** Пул збирає від багатьох користувачів → досягає 10,000+ TON → стейкає

### Тестування:
1. Спочатку деплой на **testnet**
2. Перевірка всіх операцій (deposit, withdraw)
3. Тільки потім на **mainnet**

---

## 🔄 Наступні кроки після патчу

1. ✅ Застосувати патч параметрів
2. ⏳ Скомпілювати контракт (`func/build.sh`)
3. ⏳ Задеплоїти на testnet
4. ⏳ Протестувати через frontend
5. ⏳ Оновити `TON_POOL_ADDRESS` в backend `.env`

---

## 📊 Вплив на користувачів

### До змін:
- Мін. внесок: 100 TON (~$300-500)
- Макс. учасників: 40
- **Тільки для великих інвесторів**

### Після змін:
- Мін. внесок: 10 TON (~$30-50) ✅
- Макс. учасників: 1000 ✅
- **Доступно для всіх** 🎉

---

## 🛠️ Технічні деталі

### Розмір даних в блокчейні:
- Кожен номінатор: ~136 bytes (адреса + amount + pending)
- 1000 номінаторів: ~136 KB
- **Вміщається в один cell** (до 1023 байт на cell, використовуємо dictionary)

### Gas витрати:
- Deposit: ~0.05 TON
- Withdraw: ~0.1 TON (може зайняти до 36 годин)
- Update validator set: ~0.2 TON

---

## ❓ FAQ

**Q: Чому мінімум 10 TON?**
A: Баланс між доступністю та захистом від спаму. Можна знизити до 1 TON, але збільшаться gas витрати.

**Q: Що якщо учасників більше 1000?**
A: Нові депозити будуть відхилені. Можна створити другий пул.

**Q: Як працює з 10 TON мін., якщо elector потребує 10,000 TON?**
A: Пул збирає від усіх учасників. Коли загальна сума > 10,000 TON, пул може стейкати.

**Q: Чи можна змінити після деплою?**
A: **НІ!** Immutable контракт. Параметри встановлюються один раз при створенні.
